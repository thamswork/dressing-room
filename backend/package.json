{
  "name": "dressing-room-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute dressing-room-db --local --file=../db/schema.sql",
    "db:migrate:remote": "wrangler d1 execute dressing-room-db --remote --file=../db/schema.sql"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.103.0"
  }
}
