# --- Next.js frontend (public, safe to expose in the client bundle) ---
NEXT_PUBLIC_WORKER_API_BASE=https://dressing-room-worker.YOUR_SUBDOMAIN.workers.dev
NEXT_PUBLIC_GEONAMES_USERNAME=your_geonames_username

# --- Cloudflare Worker (set with `wrangler secret put`, do not commit real values) ---
# ASTROLOGY_API_KEY=your_astrology_api_key
